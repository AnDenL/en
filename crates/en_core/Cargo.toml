[package]
name = "en_core"
version = "0.1.0"
edition = "2024"

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
web-sys = { version = "0.3", features = ["Window", "Response", "Storage"] }

[dependencies]
wgpu = "29.0"
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
winit = { version = "0.30", default-features = false, features = ["rwh_06", "x11", "wayland"] }
glam = { version = "0.32", features = ["bytemuck"] }
bevy_ecs = { version = "0.18", default-features = false, features = ["bevy_reflect"] }
bevy_reflect = { version = "0.18", default-features = false }
pollster = "0.4"
bytemuck = { version = "1.25", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

smart-default = "0.7"

inventory = "0.3"
en_macros = { path = "../en_macros" }
en_render ={ path = "../en_render" }
